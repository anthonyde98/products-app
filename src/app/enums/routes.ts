export enum RelativeRoutes {
    information = "information",
    products = "/",
    set = "set"
}

export enum AbsoluteRoutes {
    root = "/",
    information = "products/information/",
    products = "products",
    set = "products/set"
}

export enum APIRoutes {
    rootDummy = "https://dummyjson.com",
    products = "/products",
    categories = "/categories",
    category = "/category",
    search = "/search",
    imagesCloudinary = "https://api.cloudinary.com/v1_1/dlobfizxs/image/upload"
}

